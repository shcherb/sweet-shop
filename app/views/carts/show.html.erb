<% if notice %>
  <p id="notice"><%= notice %></p>
<% end %>

<% @cart.line_items.each do |item| %>
  <li><%= itmem.quietly %> &times; <%= item.product.title %></li>
<% end %>

<%= button_to 'Emty cart', @cart, method: :delete, confirm: 'Are you sure ?' %>

<%= link_to 'Edit', edit_cart_path(@cart) %> |
<%= link_to 'Back', carts_path %>
